**/dist/**
**/node_modules/**
**/coverage/**

# Los worktrees de los agentes traen una copia entera del repositorio; sus ficheros no son
# nuestros y `prototipo/**` de abajo no casa con la copia anidada. Ver eslint.config.mjs.
.claude/worktrees/**
pnpm-lock.yaml
apps/api/prisma/migrations/**

# Docs are hand-written prose: line breaks and tables are deliberate.
**/*.md

# Salidas de Playwright.
apps/web/test-results/**
apps/web/playwright-report/**

# La maqueta que devolvio Figma Make (2026-09-03): codigo de terceros pendiente de
# adaptar, con su propio package.json y fuera del workspace. Se ignora a proposito y
# **temporalmente** — mientras estuvo dentro, `pnpm verify` fallaba y bloqueaba todos
# los commits del repositorio. Esta entrada se borra cuando la maqueta se adopte.
prototipo/**
